export enum QuickGameState {
  IDLE = 'idle',
  SPINNING = 'spinning',
  STOPPING = 'stopping',
  AWARD_WINS = 'award_wins',
}

export enum QuickGameStateEvent {
  ENTER = 'enter',
  EXIT = 'exit',
}
